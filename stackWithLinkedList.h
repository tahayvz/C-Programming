#ifndef STACK_WITH_LINKED_LISTS
#define STACK_WITH_LINKED_LISTS
#include "LinkedLists.h"

void popLL(node **);
node * pushLL(node *,int);
void printLL(node *);

void stackWithLinkedList(void);

#endif // STACK_WITH_LINKED_LISTS
